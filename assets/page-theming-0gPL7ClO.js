import{A as e,Ct as t,M as n,O as r,h as i,m as a,n as o,p as s,s as c,u as l}from"./runtime-core.esm-bundler-CyDMPmLI.js";import{jt as u}from"./index-Dq2l2Sk3.js";var d={class:`prose prose-slate dark:prose-invert *:animate-fade-in *:delay`},f=[`href`],p=`:root {
	--primary-500: oklch(0.6 0.2 250);
}`,m=`@import "@lewishowles/components/source";`,h=`<html class="dark">`,g={__name:`page-theming`,setup(g){let{setTitle:_}=u();_(`Theming`);let v=[{file:`main.css`,description:`The entry point. Pulls in Tailwind and imports the stylesheets below.`},{file:`theme.css`,description:`Colour scales and semantic tokens — the file you override to re-theme.`},{file:`buttons.css`,description:`Button shapes and variants.`},{file:`form.css`,description:`Form control styling.`},{file:`font.css`,description:`Font faces and typography tokens.`},{file:`animation.css`,description:`Keyframes and animation helpers.`}];function y(e){return`/css/${e}`}return(u,g)=>{let _=n(`code-block`);return r(),l(`div`,d,[g[0]||=s(`<h1>Theming</h1><p> The library is themed with CSS variables, allowing you to re-colour everything from your own stylesheet. There are two layers to the system, and most of the time you&#39;ll only need to touch the first. </p><h2>The two layers</h2><p> The first layer is a set of named colour scales. The five scales; primary, danger, warning, success, and info; each run from <code>50</code> (the lightest shade) to <code>950</code> (the darkest). This is where colour actually lives, and it&#39;s the layer you override if you need to. </p><p> The second layer is the semantic tokens. Rather than asking for &quot;purple 800&quot;, components ask for &quot;the primary fill&quot; or &quot;normal body text&quot;. Each token picks a sensible shade from the scales, and it picks the right shade for light or dark mode. </p><h2>Re-theming</h2><p> To re-theme, set the scale variables you care about in your own <code>:root</code> , after the library&#39;s stylesheet has loaded. Your values win: </p>`,7),i(_,{code:p}),g[1]||=c(`p`,null,[a(` That single line re-points everything built on `),c(`code`,null,`--primary-500`),a(` . To shift the whole brand, set the shades that your brand relies on. `)],-1),g[2]||=c(`h2`,null,`Using the stylesheets`,-1),g[3]||=c(`p`,null,` The styles are built with Tailwind, so your project needs Tailwind (version 4) as well. Point it at the library so it can generate the utility classes the components rely on. Import this into your Tailwind entry stylesheet: `,-1),i(_,{code:m}),g[4]||=c(`p`,null,[a(` Each of the library's stylesheets is published so that you can start from a working copy rather than a blank file. Overriding tokens covers most re-theming, but to change how a component itself looks, from button shapes to form fields, copy the matching stylesheet and edit it. `),c(`code`,null,`main.css`),a(` is the entry point and imports the rest: `)],-1),c(`ul`,null,[(r(),l(o,null,e(v,e=>c(`li`,{key:e.file},[c(`a`,{href:y(e.file),download:``},[c(`code`,null,t(e.file),1)],8,f),a(` — `+t(e.description),1)])),64))]),g[5]||=s(`<h2>Colour scales</h2><p> Every family follows the same pattern — <code>--{name}-50</code> through <code>--{name}-950</code> . The defaults map to a Tailwind palette. </p><table><thead><tr><th>Family</th><th>Variables</th><th>Default palette</th></tr></thead><tbody><tr><td>Primary</td><td><code>--primary-50</code> … <code>--primary-950</code></td><td>Purple</td></tr><tr><td>Danger</td><td><code>--danger-50</code> … <code>--danger-950</code></td><td>Red</td></tr><tr><td>Warning</td><td><code>--warning-50</code> … <code>--warning-950</code></td><td>Yellow</td></tr><tr><td>Success</td><td><code>--success-50</code> … <code>--success-950</code></td><td>Green</td></tr><tr><td>Info</td><td><code>--info-50</code> … <code>--info-950</code></td><td>Blue</td></tr></tbody></table><h2>Semantic tokens</h2><p> Each intent (primary, danger, warning, success, info) gives you four roles. Reach for the utility rather than a raw shade wherever you can, because the utility keeps light and dark mode correct without any extra work from you. </p><table><thead><tr><th>Role</th><th>Utility</th><th>What it&#39;s for</th></tr></thead><tbody><tr><td>Fill</td><td><code>bg-primary</code></td><td>A solid background, such as a primary button or a danger banner</td></tr><tr><td>Text</td><td><code>text-primary</code></td><td>Coloured text and icons</td></tr><tr><td>Border</td><td><code>border-primary</code></td><td>A border that matches the text shade</td></tr><tr><td>Subtle</td><td><code>bg-primary-subtle</code></td><td>A soft tint behind content, like a quiet alert</td></tr><tr><td>Foreground</td><td><code>text-primary-foreground</code></td><td>Text that sits on top of a fill, kept readable</td></tr></tbody></table><p> Swap <code>primary</code> for any intent to get the same five utilities. Behind each one is a meaningful shade, which changes between light and dark mode: </p><table><thead><tr><th>Intent</th><th>Fill (light → dark)</th><th>Text (light → dark)</th></tr></thead><tbody><tr><td>Primary</td><td>800 → 500</td><td>800 → 300</td></tr><tr><td>Danger</td><td>700 → 500</td><td>800 → 200</td></tr><tr><td>Warning</td><td>300 → 300</td><td>800 → 200</td></tr><tr><td>Success</td><td>600 → 500</td><td>800 → 200</td></tr><tr><td>Info</td><td>600 → 500</td><td>800 → 200</td></tr></tbody></table><p> The subtle wash uses the <code>50</code> shade in light mode and a half-transparent <code>500</code> in dark mode. The foreground is white, except on the light warning fill, where it&#39;s near-black so the text stays legible. </p><h2>Neutral tokens</h2><p> Surfaces, body text, and borders that aren&#39;t tied to an intent have their own tokens. These carry the bulk of a page&#39;s appearance: </p><table><thead><tr><th>Utility</th><th>Role</th><th>Light</th><th>Dark</th></tr></thead><tbody><tr><td><code>bg-surface</code></td><td>Page and card background</td><td><code>white</code></td><td><code>grey-950</code></td></tr><tr><td><code>bg-surface-subtle</code></td><td>A slightly raised panel</td><td><code>grey-50</code></td><td><code>white/10%</code></td></tr><tr><td><code>bg-surface-sunken</code></td><td>A recessed well</td><td><code>grey-100</code></td><td><code>white/20%</code></td></tr><tr><td><code>text-content</code></td><td>Body text</td><td><code>grey-600</code></td><td><code>grey-50</code></td></tr><tr><td><code>text-content-strong</code></td><td>Headings and emphasis</td><td><code>grey-950</code></td><td><code>white</code></td></tr><tr><td><code>text-content-muted</code></td><td>Secondary text</td><td><code>grey-500</code></td><td><code>white/60%</code></td></tr><tr><td><code>border-border</code></td><td>Default border</td><td><code>grey-200</code></td><td><code>white/20%</code></td></tr><tr><td><code>border-border-strong</code></td><td>A stronger border</td><td><code>grey-300</code></td><td><code>white/30%</code></td></tr></tbody></table><h2>Numbered shades and roles are different things</h2><p>Two utilities look alike but aren&#39;t the same thing:</p><p><code>bg-primary-500</code> is one specific step on the scale. The scale steps are available wherever Tailwind expects a colour: backgrounds, text, borders, and rings. Reach for these when you need an exact shade. </p><p><code>bg-primary</code> is a role. It already holds the right shade for the job and swaps itself between light and dark mode. Use roles for almost everything, and drop down to a numbered shade only when a role doesn&#39;t cover what you need. </p><h2>Dark mode</h2><p> Dark mode is class-based, and it works the way it always has. Add the <code>dark</code> class to a parent element (usually <code>&lt;html&gt;</code> ) and every semantic token flips to its dark value. There&#39;s nothing to restyle. The tokens carry the dark appearance themselves. </p>`,18),i(_,{code:h}),g[6]||=c(`p`,null,` If you re-theme a scale, dark mode keeps working. The dark values read from the same scales you override, so your brand colour follows through into both modes. `,-1)])}}};export{g as default};