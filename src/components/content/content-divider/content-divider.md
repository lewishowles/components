# `content-divider`

A standardised divider between content.

## Props

### `tag`

- type: `string`
- default: `div`

The tag to use for this divider. Generally, this is a `div` for visual dividers, and `hr` for content dividers.

## Examples

### Basic divider

```html
<content-divider />
```
